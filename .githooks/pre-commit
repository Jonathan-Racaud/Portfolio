#!/usr/bin/env bash
set -e

if ! command -v bun >/dev/null 2>&1; then
  echo "pre-commit: bun not found on PATH — skipping astro check." >&2
  exit 0
fi

echo "pre-commit: running astro check…"
bun run astro check
